{
  "name": "skyzonee-websocket-server",
  "version": "2.0.0",
  "description": "WebSocket server for skyzonee e-commerce chat system with MongoDB persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install",
    "test": "node -c server.js && node -c database.js && echo All syntax checks passed!"
  },
  "keywords": [
    "websocket",
    "socket.io",
    "express",
    "chat",
    "real-time"
  ],
  "author": "skyzonee",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "mongodb": "^6.20.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
